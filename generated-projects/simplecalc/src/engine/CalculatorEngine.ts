export const CalculatorEngine = {
  // Placeholder methods; actual implementations are mocked in tests.
  parseExpression: (expr: string) => {
    return { error: { message: 'Not implemented' } };
  },
  evaluateExpression: (expr: string) => {
    return { error: { message: 'Not implemented' } };
  },
};
