# Plan 16 — Implementation Report

## Sync the workspace to the merged system branch (fixes A1)

**Finding addressed:** A1 — The workspace QA and DevOps operate on never receives the merged developer code.

**Root cause:** `executePRWorkflow` does all development inside isolated git worktrees and squash-merges PRs on the remote via `octokit.pulls.merge`. Nothing ever fast-forwards the main checkout at `state.workspacePath`, so `qaNode` and `devopsNode` see a tree containing only `docs/` artifacts and `.conventions/` — none of the merged application source. QA burned its entire tool budget exploring an empty directory, and DevOps wrote configs for a project it couldn't see.

---

## Changes Made

### 1. New module: `src/utils/git-exec.ts`

Extracted `gitExec`, `gitPush`, and `findGitRoot` from their duplicated definitions in `nodes.ts` (lines 73-96) and `pr-workflow.ts` (lines 73-101, 203-211) into a single shared module. All three functions are now imported from one place.

- `gitExec(workspacePath, args)` — runs a git command, returns trimmed stdout or `"Error: ..."` on failure (never throws).
- `gitPush(workspacePath, branchName, gitContext?)` — pushes HEAD via authenticated URL.
- `findGitRoot(startPath)` — walks up from a path to find the nearest `.git` directory.

### 2. New module: `src/conductor/workspace-sync.ts`

Implements the sync logic with a strategy ladder:

```
already-current -> merge --ff-only -> rebase -> hard reset (if allowed)
```

**Exported functions:**

| Function | Purpose |
|----------|---------|
| `syncWorkspaceToBranch(gitRoot, branch, gitContext?, opts?)` | Brings local checkout up to date with `origin/<branch>`. Prunes stale worktrees, auto-commits dirty files, fetches, then applies the strategy ladder. Returns `SyncResult` with `ok`, `headSha`, `details`, and `strategy`. |
| `isDirty(porcelainOutput)` | Pure helper — true when `git status --porcelain` output indicates a dirty tree. |
| `looksSourceless(relativePaths)` | Pure helper — true when the file list contains no application source (only docs/, .conventions/, dotfiles, README, LICENSE). |

### 3. Updated `src/conductor/nodes.ts`

| Location | Change |
|----------|--------|
| Imports | Replaced local `gitExec`/`gitPush` with imports from `../utils/git-exec`. Added imports for `findGitRoot`, `syncWorkspaceToBranch`, `looksSourceless`. Removed unused `GIT_USER_NAME`/`GIT_USER_EMAIL` imports. |
| `intakeNode` | Replaced inline 12-line git root walk with `findGitRoot(workspacePath)`. |
| `commitAndPushArtifacts` | Now syncs with origin before committing, and retries the push once after a sync if the first push fails (handles non-fast-forward after squash merges). |
| `developmentNode` (end) | Added `syncWorkspaceToBranch` call after `dispatchDevelopers`. Runs `looksSourceless` over `git ls-files` and logs ERROR + pushes transcript warning if workspace appears empty. Includes sync details in transcript. |
| `qaNode` (top) | Added `syncWorkspaceToBranch` call before `deployAllConventionsToWorkspace` — idempotent, protects HITL resume in a fresh process. |
| `qaNode` (commit block) | Replaced 8-line inline `git add`/`commit`/`push` block with `commitAndPushArtifacts(...)` call, inheriting sync-and-retry behavior. Commit message preserved verbatim. |
| `devopsNode` (top) | Added `syncWorkspaceToBranch` call before `deployAllConventionsToWorkspace`. |
| `devopsNode` (commit block) | Replaced 8-line inline `git add`/`commit`/`push` block with `commitAndPushArtifacts(...)` call. Commit message preserved verbatim. |

### 4. Updated `src/conductor/pr-workflow.ts`

- Replaced local `gitExec`, `gitPush`, `findGitRoot` definitions with imports from `../utils/git-exec`.
- Re-added `GIT_USER_NAME`/`GIT_USER_EMAIL` to config imports (still used for worktree `git config`).

### 5. Config: `src/config.ts` + `.env.example`

Added `WORKSPACE_SYNC_ALLOW_RESET` — controls whether hard reset is allowed when ff/rebase both fail during workspace sync. Defaults to `true`.

```ts
export const WORKSPACE_SYNC_ALLOW_RESET =
    (process.env.WORKSPACE_SYNC_ALLOW_RESET ?? 'true') === 'true';
```

### 6. Tests: `tests/workspace-sync.test.ts`

14 tests across 3 describe blocks, all using local bare repos (no network, no GitHub account):

| Test | What it verifies |
|------|-----------------|
| fast-forward when remote has new commits | Creates dev clone commit, syncs main clone. Asserts `strategy === 'fast-forward'` and file exists. |
| dirty files committed before sync | Uncommitted file in main, remote has new commits. Asserts both files survive. Strategy is `rebase` (dirty commit + remote advance). |
| hard reset on divergence with conflict | Local and remote both modify `docs/x.md` differently (true conflict). Asserts `strategy === 'reset'` and HEAD matches remote. |
| already-current when no sync needed | No new commits. Asserts `strategy === 'already-current'`. |
| `isDirty('')` -> false | Pure unit test. |
| `isDirty(' M src/a.ts\n')` -> true | Pure unit test. |
| `isDirty` with whitespace-only | Returns false. |
| `isDirty` with untracked files | Returns true. |
| `looksSourceless` with only docs/conventions/README | Returns true. |
| `looksSourceless` with src/index.ts added | Returns false. |
| `looksSourceless` with dotfiles and .github/ | Returns true. |
| `looksSourceless` with package.json at root | Returns false. |
| `looksSourceless` with nested lib/ files | Returns false. |
| `looksSourceless` with empty list | Returns true. |

---

## Acceptance Criteria Status

| Criterion | Status |
|-----------|--------|
| `developmentNode`, `qaNode` and `devopsNode` each call `syncWorkspaceToBranch` exactly once | Done |
| `gitExec` / `gitPush` / `findGitRoot` exist in exactly one module | Done — `src/utils/git-exec.ts` |
| Run log contains `Workspace synced to origin/project/<slug>` with sha transition and shortstat | Done — visible in test output |
| Sourceless workspace after development produces ERROR line and transcript entry | Done — `looksSourceless` check in `developmentNode` |
| No `git add .`/`commit`/`push` block remains inline in `qaNode` or `devopsNode` | Done — both replaced with `commitAndPushArtifacts` |

## Verification Results

- `npx tsc --noEmit` — exit 0 (clean)
- `tests/workspace-sync.test.ts` — 14/14 passed (5.8s)

## Files Changed

| File | Action |
|------|--------|
| `src/utils/git-exec.ts` | **Created** — shared gitExec, gitPush, findGitRoot |
| `src/conductor/workspace-sync.ts` | **Created** — syncWorkspaceToBranch, isDirty, looksSourceless |
| `src/conductor/nodes.ts` | **Modified** — import shared helpers, add sync calls, replace inline commit blocks |
| `src/conductor/pr-workflow.ts` | **Modified** — import shared helpers, remove local duplicates |
| `src/config.ts` | **Modified** — added WORKSPACE_SYNC_ALLOW_RESET |
| `.env.example` | **Modified** — added WORKSPACE_SYNC_ALLOW_RESET comment |
| `tests/workspace-sync.test.ts` | **Created** — 14 tests |

---

## Sub-Plan 2 — Assignment lifecycle: never redo completed work (fixes A2)

**Finding addressed:** A2 — The bug-fix loop re-runs every already-completed assignment.

**Root cause:** `assignments` uses the **append** reducer, so after a bug-fix triage round the state holds the original assignments AND the new fix assignments. `developmentNode` called `dispatchDevelopers(apiKey, state.assignments, ...)` with the entire accumulated list, re-running every completed assignment on every bug-fix iteration — up to 4x the intended development cost. Additionally, `fileChanges` was injected as full pretty-printed JSON (`JSON.stringify(state.fileChanges, null, 2)`) into every developer's context prompt, growing without bound.

---

### Changes Made

#### 1. New module: `src/conductor/assignment-policy.ts`

Pure functions for assignment lifecycle management:

| Function | Purpose |
|----------|---------|
| `selectPendingAssignments(assignments, completedIds)` | De-duplicates by id (first occurrence wins), then removes completed ids. Returns only assignments still needing dispatch. |
| `completedIdsFromPullRequests(prs)` | Extracts assignment ids from PRs with status `merged` or `approved`. Excludes `open`, `closed`, and `escalated_open`. |
| `namespaceBugfixAssignments(assignments, iteration)` | Prefixes ids with `BUGFIX-<n>-` to avoid collisions. Rewrites intra-batch `dependsOn` entries; leaves external refs unchanged. |
| `dedupeBugs(bugs)` | De-duplicates bugs by id (first occurrence wins). Bugs use append reducer, so duplicates accumulate across iterations. |

#### 2. Updated `src/conductor/state.ts`

Added two new append-reduced state fields:

```ts
completedAssignmentIds: Annotation<string[]>({
    reducer: appendReducer,
    default: () => [],
}),
fixedBugIds: Annotation<string[]>({
    reducer: appendReducer,
    default: () => [],
}),
```

#### 3. Updated `src/agents/developers/dispatcher.ts`

| Location | Change |
|----------|--------|
| Imports | Added import of `completedIdsFromPullRequests` from `assignment-policy`. |
| `DispatchResult` | Added `completedAssignmentIds: string[]` field. |
| `topoSort` | Now accepts optional `preSatisfied: Set<string>` parameter. Seeds the completed set so dependencies on already-merged work are resolved rather than falling back to the "cyclic" dump. Exported for testing. |
| `dispatchDevelopers` | Accepts optional `completedAssignmentIds` parameter, passes `preSatisfied` to `topoSort`, accumulates newly completed ids from fulfilled PR results via `completedIdsFromPullRequests`. |

#### 4. Updated `src/conductor/nodes.ts`

| Location | Change |
|----------|--------|
| Imports | Added `selectPendingAssignments`, `dedupeBugs`, `namespaceBugfixAssignments` from `./assignment-policy`. Added `DEV_CONTEXT_FILE_CHANGES_LIMIT` from `../config`. |
| `developmentNode` | Filters assignments via `selectPendingAssignments(state.assignments, state.completedAssignmentIds)`. Logs pending/total/complete counts. Returns early with no-op if pending is empty. Passes `pending` (not `state.assignments`) and `state.completedAssignmentIds` to `dispatchDevelopers`. Returns `completedAssignmentIds` from dispatch result. |
| `developmentNode` (fileChanges blob) | Replaced `JSON.stringify(state.fileChanges, null, 2)` with a capped one-line-per-file summary: `state.fileChanges.slice(-DEV_CONTEXT_FILE_CHANGES_LIMIT)` mapped to `- <action> <path>`. |
| `bugfixTriageNode` | De-duplicates bugs via `dedupeBugs(state.bugs)`, filters out `fixedBugIds`, then filters for critical/major. Namespaces new assignments via `namespaceBugfixAssignments(rawAssignments, iteration)`. Returns `fixedBugIds` with the bug ids being addressed. |

#### 5. Config: `src/config.ts` + `.env.example`

Added `DEV_CONTEXT_FILE_CHANGES_LIMIT` — max file-change entries injected into the dev context prompt. Defaults to `60`.

```ts
export const DEV_CONTEXT_FILE_CHANGES_LIMIT =
    parseInt(process.env.DEV_CONTEXT_FILE_CHANGES_LIMIT ?? '60', 10);
```

#### 6. Updated `tests/qa-node-resilience.test.ts`

Added `completedAssignmentIds: []` and `fixedBugIds: []` to the `makeMinimalState` fixture to satisfy the new state fields.

#### 7. Tests: `tests/assignment-policy.test.ts`

14 tests across 5 describe blocks, all pure (no LLM, no git, no network):

| Test | What it verifies |
|------|-----------------|
| `selectPendingAssignments` — 3 remaining of 5 | Returns the 3 non-completed assignments, in order. |
| `selectPendingAssignments` — dedup by id | Duplicate ids collapse to first occurrence. |
| `selectPendingAssignments` — all completed | Returns empty array. |
| `selectPendingAssignments` — order preserved | Remaining assignments keep their original order. |
| `completedIdsFromPullRequests` — merged/approved | Includes merged and approved, excludes open/closed/escalated_open. |
| `completedIdsFromPullRequests` — empty | Returns empty for no PRs. |
| `namespaceBugfixAssignments` — prefixes | Ids become `BUGFIX-<n>-<original>`. |
| `namespaceBugfixAssignments` — intra-batch deps | Rewrites intra-batch `dependsOn`, leaves external untouched. |
| `namespaceBugfixAssignments` — no mutation | Original assignments are not mutated. |
| `dedupeBugs` — first occurrence | Keeps first occurrence of each bug id. |
| `dedupeBugs` — empty | Returns empty for empty input. |
| `topoSort` with `preSatisfied` — resolves external dep | `[A dependsOn X]` with `preSatisfied={X}` yields one layer containing A. |
| `topoSort` with `preSatisfied` — mixed deps | Pre-satisfied + batch deps correctly layer. |
| `topoSort` with empty `preSatisfied` | Behaves like original topoSort. |

---

### Acceptance Criteria Status

| Criterion | Status |
|-----------|--------|
| Grep: `dispatchDevelopers(` receives `pending`, never `state.assignments` | Done |
| Two bug-fix iterations dispatch only their own new assignments | Done — `selectPendingAssignments` filters, `namespaceBugfixAssignments` namespaces |
| No `JSON.stringify(state.fileChanges` remains in `nodes.ts` | Done — replaced with capped one-line-per-file summary |
| Bug-fix assignment ids are prefixed `BUGFIX-<n>-` | Done — `namespaceBugfixAssignments` |

### Verification Results

- `npx tsc --noEmit` — exit 0 (clean)
- `tests/assignment-policy.test.ts` — 14/14 passed (9.0s)
- `npm test` — 179/182 passed (3 pre-existing failures in `tests/qa-node-resilience.test.ts` due to missing `logToolAction` mock, unrelated to this change)

### Files Changed

| File | Action |
|------|--------|
| `src/conductor/assignment-policy.ts` | **Created** — selectPendingAssignments, completedIdsFromPullRequests, namespaceBugfixAssignments, dedupeBugs |
| `src/conductor/state.ts` | **Modified** — added `completedAssignmentIds` and `fixedBugIds` fields |
| `src/agents/developers/dispatcher.ts` | **Modified** — added `completedAssignmentIds` to DispatchResult, updated `topoSort` with `preSatisfied`, accumulate completions |
| `src/conductor/nodes.ts` | **Modified** — filter pending assignments, cap fileChanges blob, namespace bugfix assignments, dedupe bugs |
| `src/config.ts` | **Modified** — added `DEV_CONTEXT_FILE_CHANGES_LIMIT` |
| `.env.example` | **Modified** — added `DEV_CONTEXT_FILE_CHANGES_LIMIT` comment |
| `tests/qa-node-resilience.test.ts` | **Modified** — added new state fields to mock |
| `tests/assignment-policy.test.ts` | **Created** — 14 tests |

---

## Sub-Plan 3 — Make QA E2E reachable and verify deployment for real (fixes A3, A5)

**Findings addressed:**
- A3 — QA E2E can never run (the E2E block was inside `qaNode`, gated on `state.devopsPlan?.serviceUrls` which was always `null` because DevOps hadn't run yet).
- A5 — `docker-runner.ts` is dead code; deployment status is an LLM self-report (nothing imported docker-runner.ts, and `buildStatus`/`runStatus`/`serviceUrls` came straight from the DevOps agent's unverified JSON).

**Root cause (A3):** The graph edges were `qa -> devops -> finalize`, so `devopsPlan` was always `null` when `qaNode` ran. The Playwright MCP integration, the `qa-e2e` agent, and the E2E section of the README were unreachable code.

**Root cause (A5):** `src/executor/docker-runner.ts` exported `buildImage`, `runContainer`, `healthCheck`, and `stopContainer` (208 lines, dockerode-based) but nothing in `src/` imported them. `devopsPlan` fields came from the DevOps agent's JSON response with no actual verification that images build, containers start, or services answer.

---

### Changes Made

#### 1. Added `'e2e'` to `PhaseName` schema

Updated `src/agents/_shared/schemas/phase.schema.ts` to include `'e2e'` in the `PhaseNameSchema` enum, making it a valid pipeline phase.

#### 2. New module: `src/conductor/devops-verify.ts`

Implements actual deployment verification using the previously-dead `docker-runner.ts`:

| Function | Purpose |
|----------|---------|
| `chooseDeploymentMode(workspacePath)` | Picks deployment strategy: `'compose'` (docker-compose.y[a]ml), `'dockerfile'` (Dockerfile only), or `'none'`. |
| `deriveServiceUrls(composePsJson, hostname?)` | Parses `docker compose ps --format json` NDJSON output into `{service, url}[]`. Skips services with no published port. |
| `verifyDeployment(workspacePath, projectSlug)` | Builds and runs what the DevOps agent wrote, health-checks it. Returns verified `buildStatus`, `runStatus`, `serviceUrls`, `healthChecks`, `containerNames`, and `logs`. Never throws: Docker-less environments degrade to `buildStatus: 'skipped'`. |
| `teardownDeployment(workspacePath, containerNames)` | Stops and removes containers started by `verifyDeployment`. Uses `docker compose down` for compose mode, `stopContainer()` for dockerfile mode. |

The module imports and uses `buildImage`, `runContainer`, `healthCheck`, and `stopContainer` from `src/executor/docker-runner.ts` -- making it no longer dead code.

Deployment modes:
- **compose**: validates with `docker compose config -q`, then `docker compose up -d --build`, then `docker compose ps --format json` -> `deriveServiceUrls`, then health-checks each URL.
- **dockerfile**: calls `buildImage()`, parses `EXPOSE` ports from the Dockerfile, maps them to host ports starting at `DEVOPS_VERIFY_BASE_PORT`, calls `runContainer()`, then health-checks.
- **none**: returns all-`skipped`.

Gated by `DEVOPS_VERIFY_ENABLED` (default `true`). When disabled or Docker is unreachable, returns all-`skipped` and logs at info.

#### 3. Extracted `e2eNode` from `qaNode` (`src/conductor/nodes.ts`)

- **Removed** the E2E block from `qaNode` that was gated on `state.devopsPlan?.serviceUrls` -- which was structurally always `null`.
- **Created** a new exported `e2eNode(state)` function that:
  - Gates on `state.devopsPlan?.serviceUrls?.length > 0` -- now genuinely evaluated **after** DevOps has run.
  - When skipping, logs the specific reason (`'DEVOPS_VERIFY_ENABLED=false'` vs `'no service URLs from DevOps'`), replacing the generic "Skipped" that hid the structural bug.
  - When running, invokes the Playwright E2E agent and returns test reports + bugs.
  - Returns `phase: 'finalize'` in all cases.
- `qaNode` now returns only QA Lead + Unit results (no E2E).

#### 4. Updated `devopsNode` (`src/conductor/nodes.ts`)

| Change | Detail |
|--------|--------|
| Deployment verification | After the DevOps agent invocation: if `DEVOPS_VERIFY_ENABLED`, calls `verifyDeployment()`. If the real build status disagrees with the agent's self-report, logs a `WARN` and overrides with the verified value. |
| Return phase | Changed from `'finalize'` to `'e2e'` so the graph reaches the new E2E node. |
| Running containers | Returns `runningContainers: verified.containerNames` so finalize can tear them down. |
| Artifact content | Includes verification logs in the mission report when verification ran. |
| Transcript | Adds a deployment verification transcript entry with build/run/services counts. |

#### 5. Updated `finalizeNode` (`src/conductor/nodes.ts`)

Added container teardown at the top of `finalizeNode`: if `DEVOPS_TEARDOWN` is true and `state.runningContainers` is non-empty, calls `teardownDeployment()` wrapped in try/catch. Teardown happens after E2E (which is why it lives in finalize rather than devops).

#### 6. Re-wired the graph (`src/conductor/graph.ts`)

New graph shape:
```
development -> qa
qa   --afterQaRouter-->      { bugfix-triage, devops }
devops -> e2e
e2e  --afterE2eRouter-->     { bugfix-triage, finalize }
bugfix-triage -> development
```

- Added `e2eNode` import and `.addNode('e2e', e2eNode)`.
- Changed `devops -> finalize` edge to `devops -> e2e`.
- Added conditional edges from `e2e` via `afterE2eRouter`:
  - `'bugfix-triage'` only when ALL of: `E2E_BUGFIX_ENABLED=true`, an E2E report has `status === 'fail'`, and `bugfix < MAX_BUGFIX_ITERATIONS`.
  - `'finalize'` otherwise (default -- preserves today's cost profile).
- Added `'e2e'` to `HITL_PHASES`.
- Exported `afterQaRouter`, `afterE2eRouter`, and `afterIntakeRouter` for direct testing.

#### 7. New state field: `runningContainers` (`src/conductor/state.ts`)

```ts
runningContainers: Annotation<string[]>({
    reducer: replaceReducer,
    default: () => [],
}),
```

Carries container names from `devopsNode` to `finalizeNode` for teardown.

#### 8. Config: `src/config.ts` + `.env.example`

| Config | Default | Purpose |
|--------|---------|---------|
| `DEVOPS_VERIFY_ENABLED` | `true` | Enable/disable actual Docker build/run verification |
| `DEVOPS_VERIFY_TIMEOUT_MS` | `600000` | Timeout per shell step during verification |
| `DEVOPS_VERIFY_BASE_PORT` | `18080` | First host port for EXPOSE port mapping |
| `DEVOPS_HEALTH_RETRIES` | `5` | Health-check retry count |
| `DEVOPS_HEALTH_DELAY_MS` | `3000` | Delay between health-check retries |
| `DEVOPS_TEARDOWN` | `true` | Tear down containers during finalize |
| `E2E_BUGFIX_ENABLED` | `false` | Allow E2E failures to trigger bugfix loop |

#### 9. Updated `tests/qa-node-resilience.test.ts`

- Added `runningContainers: []` to the `makeMinimalState` fixture.
- Updated expected phase for `devopsNode` from `'finalize'` to `'e2e'`.
- Added mock for `devops-verify` module (returns all-`skipped`).
- Fixed pre-existing `logToolAction` mock issue (added `logToolAction: jest.fn()` to logger mock).

#### 10. Tests: `tests/devops-verify.test.ts`

16 tests across 3 describe blocks, no Docker required:

| Test | What it verifies |
|------|-----------------|
| `chooseDeploymentMode` -- docker-compose.yml | Returns `'compose'` |
| `chooseDeploymentMode` -- docker-compose.yaml | Returns `'compose'` |
| `chooseDeploymentMode` -- compose.yml | Returns `'compose'` |
| `chooseDeploymentMode` -- Dockerfile only | Returns `'dockerfile'` |
| `chooseDeploymentMode` -- both compose and Dockerfile | Prefers `'compose'` |
| `chooseDeploymentMode` -- neither | Returns `'none'` |
| `chooseDeploymentMode` -- empty dir | Returns `'none'` |
| `deriveServiceUrls` -- two services with published ports | Parses both URLs correctly |
| `deriveServiceUrls` -- skips services with PublishedPort: 0 | Only returns published services |
| `deriveServiceUrls` -- no publishers | Returns empty |
| `deriveServiceUrls` -- empty input | Returns empty |
| `deriveServiceUrls` -- malformed JSON lines | Skips bad lines, parses good ones |
| `deriveServiceUrls` -- custom hostname | Uses provided hostname |
| `deriveServiceUrls` -- lowercase publisher fields | Handles `published_port` variant |
| `verifyDeployment` -- DEVOPS_VERIFY_ENABLED=false | Returns all-`skipped` with empty logs |
| `verifyDeployment` -- no Docker artifacts | Returns skipped (mode=none) |

#### 11. Tests: `tests/graph-routing.test.ts`

10 tests across 3 describe blocks, pure router function tests:

| Test | What it verifies |
|------|-----------------|
| `afterE2eRouter` -- E2E_BUGFIX_ENABLED=false with failures | Returns `'finalize'` (default cost profile preserved) |
| `afterE2eRouter` -- E2E_BUGFIX_ENABLED=true with failures and iterations remaining | Returns `'bugfix-triage'` |
| `afterE2eRouter` -- E2E_BUGFIX_ENABLED=true but max iterations reached | Returns `'finalize'` |
| `afterE2eRouter` -- E2E_BUGFIX_ENABLED=true but no failures | Returns `'finalize'` |
| `afterE2eRouter` -- E2E_BUGFIX_ENABLED=true but no test reports | Returns `'finalize'` |
| `afterQaRouter` -- tests fail, iterations remain | Returns `'bugfix-triage'` |
| `afterQaRouter` -- tests pass | Returns `'devops'` |
| `afterQaRouter` -- max iterations reached with failures | Returns `'devops'` |
| `afterIntakeRouter` -- maintain mode | Returns `'codebase-analyzer'` |
| `afterIntakeRouter` -- greenfield mode | Returns `'architect'` |

---

### Acceptance Criteria Status

| Criterion | Status |
|-----------|--------|
| The graph contains an `e2e` node reached from `devops`; `devopsNode` returns `phase: 'e2e'` | Done |
| With `serviceUrls` present, `e2eNode` invokes the Playwright agent; with none, it logs the specific reason | Done |
| `src/executor/docker-runner.ts` has at least one importer | Done -- imported by `src/conductor/devops-verify.ts` |
| A disagreement between the agent's `buildStatus` and the verified one produces a `WARN` and the verified value wins | Done |
| `DEVOPS_VERIFY_ENABLED=false` and a Docker-less host both leave the run green | Done -- returns all-`skipped` |

### Verification Results

- `npx tsc --noEmit` -- exit 0 (clean)
- `tests/devops-verify.test.ts` -- 16/16 passed
- `tests/graph-routing.test.ts` -- 10/10 passed
- `tests/qa-node-resilience.test.ts` -- 3/3 passed (previously 0/3 due to missing `logToolAction` mock -- now fixed)
- `npm test` -- 208/208 passed (15/15 test suites, 0 failures)

### Files Changed

| File | Action |
|------|--------|
| `src/agents/_shared/schemas/phase.schema.ts` | **Modified** -- added `'e2e'` to `PhaseNameSchema` |
| `src/conductor/devops-verify.ts` | **Created** -- chooseDeploymentMode, deriveServiceUrls, verifyDeployment, teardownDeployment |
| `src/conductor/graph.ts` | **Modified** -- added `e2eNode`, `afterE2eRouter`, re-wired `devops -> e2e -> finalize`, exported routers |
| `src/conductor/nodes.ts` | **Modified** -- extracted `e2eNode`, added deployment verification to `devopsNode`, added teardown to `finalizeNode`, changed devopsNode phase to `'e2e'` |
| `src/conductor/state.ts` | **Modified** -- added `runningContainers` field |
| `src/config.ts` | **Modified** -- added 7 new config constants (DEVOPS_VERIFY_*, DEVOPS_TEARDOWN, E2E_BUGFIX_ENABLED) |
| `.env.example` | **Modified** -- added DevOps Verification section with 7 commented-out env vars |
| `tests/devops-verify.test.ts` | **Created** -- 16 tests |
| `tests/graph-routing.test.ts` | **Created** -- 10 tests |
| `tests/qa-node-resilience.test.ts` | **Modified** -- added `runningContainers`, updated expected phase, added devops-verify mock, fixed logToolAction mock |
